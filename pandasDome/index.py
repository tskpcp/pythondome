import series as s
import dataframe as df
import index_objects as io
import reindexing as r
import dropping_entries_from_an_axis as defaa
import indexing_selection_and_filtering as isaf
import arithmetic_and_data_alignment as aada
import function_application_and_mapping as faam
import sorting_and_ranking as sar
import axis_indexes_with_duplicate_value as aiwdv
import intro
import correlation_and_covariance as cac
import integer_indexing as ii
import pannel_data as pd
if __name__=='__main__':
    # http://www.365yg.com/c/user/83882878410/
    #
    # 1.02:24

    #2、Series是一种类似于一维数组的对象，它由一组数据以及一组与之相关的数据标签（即索引）组成
    # s.seriesDome()
    #3、DataFrame是一个表格型的数据结构，它含有一组有序的列，每列可以是不同的值类型（数值，字符串，布尔值）
    #df.dataFrameDome()
    #4、索引对象
    # io.indexObjects()
    #5、重新索引
    #r.reindexing()
    #6、丢弃指定轴上的项
    #defaa.droppingEntriesFromAnAxis()
    #7、索引、选取和过滤
    #isaf.indexingSelectionAndFiltering()
    #8、算术运算和数据对齐
    #aada.arithmeticAndDataAlignament()
    #9、函数应用和映射
    #faam.functionApplicationAndMapping()
    #10、排序和排名
    #sar.sortingAndRanking()
    #11、带有重复值的索引
    #aiwdv.axisIndexesWithDuplicate()
    #12、汇总和计算/层次化索引
    #intro.intro()
    #13、相关系数与协方差
    #cac.correlationAndCovariance()
    #14、整数碎索引
    #ii.integerIndexing()
    #15、面板数据（Pannel）
   # pd.pannelData()
